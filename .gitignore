# 작업 하네스 런타임
.harness/

# cloud 배포 런타임 — make cloud-up 이 기록하는 EC2 인스턴스 ID(환경별, 커밋 금지)
deploy/.instance-id

# Lambda 빌드 산출물(make cloud-lambda-deploy 가 생성 — 소스에서 재생성)
deploy/lambda/build/
deploy/lambda/alarm-producer.zip

# Python
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# 환경/자격증명 (절대 커밋 금지)
.env
*.pem
*private-key*
.venv/
venv/

# SQLite job queue (MVP)
*.db

# Playwright MCP 아티팩트 + 로컬 스크린샷 (검증용, 커밋 안 함)
.playwright-mcp/
screenshots/

# web/ 대시보드 (Next.js)
web/node_modules/
web/.next/
web/.env.local
web/tsconfig.tsbuildinfo

# overnight runner 런타임 산출물 (scripts/overnight — overnight-harness 플러그인)
scripts/overnight/logs/
scripts/overnight/STOP
scripts/overnight/DONE
docs/test/history/*-overnight-review-checklist.md

# 제출물 (로컬 관리, 커밋 안 함) + Serena 캐시 + macOS
docs/submission/
.serena/
.DS_Store

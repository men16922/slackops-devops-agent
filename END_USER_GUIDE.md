# 🖥️ SlackOps 대시보드 — 쉬운 사용 설명서

> 이 문서는 **대시보드를 직접 보고 누르는 사람**을 위한 쉬운 안내입니다.
> 설치·배포·토큰 발급 같은 어려운 운영 작업은 운영자용 [USER_GUIDE.md](USER_GUIDE.md)를 보세요.

---

## 0. 한 줄 요약

Slack에서 "이 서비스 로그 좀 봐줘" 같은 명령을 내리면, AI 에이전트가 일을 하고,
**그 결과와 진행 상황을 이 대시보드에서 확인**합니다.
코드를 바꾸는 작업은 **사람이 버튼을 눌러 승인해야만** 진행됩니다.

---

## 1. 접속하기

브라우저 주소창에 입력 👉 **http://localhost:8930**

화면 위 메뉴는 두 개뿐입니다.

| 메뉴 | 무엇을 보나 |
| --- | --- |
| **Job Queue** | 어떤 명령들이 들어왔고 지금 어떤 상태인지 목록 |
| **Telemetry** | 에이전트가 돈·시간을 얼마나 썼는지 통계 |

---

## 1.5 웹에서 직접 명령 보내기 🆕

첫 화면(Job Queue) 맨 위에 **명령 입력칸**이 있습니다. Slack 없이 여기서 바로 작업을 넣을 수 있어요.

1. **드롭다운**에서 명령을 고릅니다 — `logs` / `diagnose` / `tf-review` / `pr` / `ping`
2. **인자칸**에 값을 적습니다 — 예: `logs` → 서비스명 `api`, `pr` → 변경 설명
   *(tf-review·ping 은 인자가 필요 없어 칸이 비활성화됩니다)*
3. **Send** 를 누르면 작업이 큐에 들어가 **목록 맨 위에 `pending` 상태로** 나타납니다.

> 🔒 **안전장치:** 아무 문장이나 AI에 바로 보내는 게 아니라, **정해진 명령 + 인자**로만 받습니다.
> 모르는 명령은 거부됩니다(주입 공격 방어). 실제 실행은 백그라운드 worker 가 맡습니다 —
> 이 로컬 데모에서는 worker 가 없어 작업이 `pending` 상태로 남아 "큐에 잘 들어갔다"까지 보여줍니다.

---

## 2. Job Queue — 작업 목록 (첫 화면)

"누가 무슨 명령을 시켰고, 지금 어떤 상태인지"를 한눈에 봅니다.

### 상태 색깔 읽는 법

| 뱃지 | 뜻 | 할 일 |
| --- | --- | --- |
| 🟡 **awaiting_approval** | 사람 승인을 기다리는 중 | **여기를 눌러 확인하세요** |
| 🔵 running | 실행 중 | 기다리기 |
| 🟢 done | 완료됨 | — |
| 🔴 failed | 실패함 | 상세에서 원인 확인 |

> 💡 파란색 **명령어 글씨**(예: `pr`, `diagnose`, `logs`)를 누르면 그 작업의 상세 화면으로 들어갑니다.

---

## 3. 작업 상세 — 가장 중요한 화면 ⭐

작업을 클릭하면:

- **기본 정보** — 명령어, 요청한 사람, 비용, 토큰
- **Audit Timeline (맨 아래)** — 언제 무슨 일이 있었는지 기록

### 🟡 승인 대기(awaiting_approval) 작업이면

화면에 이게 더 나타납니다.

- 📝 **diff 미리보기** — AI가 바꾸려는 코드 내용
- ✅ **Approve(승인)** / ❌ **Reject(거부)** 버튼

> **이게 안전장치의 핵심입니다.**
> AI가 코드를 만들어도, **사람이 버튼을 눌러야만** 실제로 진행됩니다.

---

## 4. 직접 해보기 (3단계)

1. 첫 화면에서 🟡 **awaiting_approval** 작업을 클릭합니다.
2. **diff를 읽어보고** → 마음에 들면 ✅ **Approve**를 누릅니다.
   → 상태가 `approved`로 바뀌고, 타임라인에 "누가 언제 승인했는지" 기록이 남습니다.
3. (재미 삼아) 새로고침 후 **같은 작업을 또 승인**해 보세요.
   → **"이미 처리된 작업입니다"** 라며 거부됩니다.
   → 같은 일을 실수로 두 번 하지 못하게 막는 안전장치입니다.

---

## 5. Telemetry — 사용량 통계

"에이전트가 자원을 얼마나 썼나"를 봅니다.

- **위쪽 카드** — 실행 횟수 / 총비용 / 토큰 / 도구 호출 / 성공률
- **아래 표** — 명령어별 집계 + 최근 실행 기록

비용이 얼마 안 드는지(보통 한 번에 몇 센트) 여기서 바로 확인할 수 있습니다.

---

## 6. 자주 묻는 질문

**Q. 목록이 비어 있어요.**
시드(샘플 데이터)가 아직 안 들어갔거나 데이터베이스 연결이 끊긴 경우입니다. 잠시 후 새로고침 해보세요.

**Q. 버튼을 눌렀는데 거부돼요.**
이미 다른 사람이 먼저 승인/거부했거나, 그 작업이 더 이상 "승인 대기" 상태가 아니기 때문입니다. 정상 동작입니다.

**Q. 데이터가 진짜인가요?**
지금 화면은 **샘플 데이터(22건)** 입니다. 실제 운영에서는 Slack 명령이 실시간으로 쌓입니다.

---

## 7. 끄기 (운영자용)

터미널에서:

```bash
cd web
docker compose down
```

데이터는 메모리에만 있어서 끄면 사라지고, 다시 켜면 샘플이 자동으로 다시 생깁니다.
